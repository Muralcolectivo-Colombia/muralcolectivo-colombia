nav a[href="#inicio"]:hover {
  color: #1E90FF; /* Azul un poco más fuerte al pasar el cursor */
  text-shadow: 0 0 5px #87CEFA; /* Le da un brillo sutil */
}

.carrete-horizontal {
  display: flex;
  overflow-x: auto;
  gap: 1rem;
  padding: 1rem 0;
  scroll-behavior: smooth;
}

.carrete-horizontal img {
  height: 200px;
  border-radius: 10px;
  flex-shrink: 0;
  object-fit: cover;
}

